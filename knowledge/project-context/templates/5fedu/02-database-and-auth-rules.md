# Pillar 2: Database Design & Supabase Auth Sync

Tài liệu này quy định các quy tắc thiết kế cơ sở dữ liệu (schema, primary/foreign keys), phân quyền, và cơ chế đồng bộ tài khoản người dùng với Supabase Auth.

---

## 1. Thiết Kế Database & Schema (Quy Tắc Cứng)

1. **Khóa Chính `id int8`**:
   - Tất cả các bảng nghiệp vụ và hệ thống của ứng dụng bắt buộc phải dùng kiểu dữ liệu `int8` (bigint) và tự động tăng dần.
   - Cú pháp tạo bảng bắt buộc:
     ```sql
     id int8 generated by default as identity primary key
     ```
   - Tuyệt đối cấm sử dụng kiểu dữ liệu `uuid` hoặc `text` cho khóa chính trừ khi có yêu cầu đặc biệt được chủ dự án phê duyệt.

2. **Khóa Ngoại (Foreign Keys)**:
   - Các cột khóa ngoại trỏ tới bảng có khóa chính `id int8` bắt buộc phải khai báo kiểu `int8` (ví dụ: `id_tai_xe int8`, `id_nhan_vien int8`, `id_dia_diem int8`).

3. **Cột Audit Mặc Định**:
   - Mọi bảng đều phải có cột `tg_tao` (timestamp default now()) và `tg_cap_nhat` (timestamp default now() kèm trigger tự động cập nhật).
   - Hầu hết các bảng nghiệp vụ phải có cột `id_nguoi_tao int8` trỏ tới bảng nhân sự, ngoại trừ các bảng danh mục hệ thống tĩnh (phòng ban, chức vụ).
   - **Kiểm tra Trigger bắt buộc**: Sau khi tạo hoặc migrate bảng, Agent phải chạy truy vấn xác nhận trigger `tg_cap_nhat` đã tồn tại:
     ```sql
     SELECT trigger_name, event_manipulation FROM information_schema.triggers WHERE event_object_table = 'ten_bang' AND trigger_name LIKE '%cap_nhat%';
     ```
     Nếu thiếu, phải tạo ngay trigger tự cập nhật `tg_cap_nhat = now()` trên sự kiện `BEFORE UPDATE`.

4. **Đặt Tên Bảng (Prefix Submenu)**:
   - Tên bảng bắt buộc viết ở dạng: `<viết-tắt-submenu>_<tên-module>`.
   - Ví dụ đúng: `hc_phieu_hanh_chinh` (Hành chính), `var_nhan_vien` (Hệ thống/Cơ bản), `vt_tai_xe` (Vận tải).
   - Ví dụ sai: `nhan-su`, `1.nhan-su`.

5. **Trình Tự Hợp Nhất & Di Chuyển Khóa Ngoại (Database Consolidations)**:
   - Khi di chuyển hoặc gộp bảng (ví dụ: gộp tài xế vào bảng nhân viên gốc), phải thực hiện trong một Transaction (`BEGIN ... COMMIT`).
   - Bắt buộc phải drop khóa ngoại cũ và tạo khóa ngoại mới ở các bảng liên đới.
   - **Xử lý dữ liệu mồ côi (Orphaned Records)**: Không tự ý xóa dữ liệu của người dùng. Di chuyển các bản ghi cũ sang bảng mới, điền các cột ràng buộc `NOT NULL` bằng giá trị mặc định hợp lệ trước khi kích hoạt lại khóa ngoại.

6. **Cấm granular RLS trên Supabase (App-side Filter Policy)**:
   - **Quy tắc**: Tuyệt đối cấm cấu hình các chính sách Row-Level Security (RLS) chi tiết ở mức database của Supabase để kiểm soát phân quyền dòng dữ liệu. 
   - **Lý do**: AI không đọc được cấu hình RLS trên Supabase, dẫn đến việc không thể tự sửa lỗi hoặc bảo trì khi có lỗi phân quyền. 
   - **Cách triển khai**: Mọi bảng chỉ cần một chính sách RLS duy nhất cho phép tất cả tài khoản đã đăng nhập CRUD (`authenticated all`). Toàn bộ việc lọc dòng dữ liệu theo phòng ban, cấp bậc, cá nhân phải được xử lý trực tiếp ở tầng code ứng dụng (API Handlers, React Hooks hoặc Frontend logic).
   - **Checklist Kiểm tra RLS bắt buộc sau mọi Migration**: Sau khi tạo bảng mới hoặc chạy migration, Agent **bắt buộc phải chạy truy vấn** kiểm tra policy trên Supabase SQL Editor:
     ```sql
     SELECT schemaname, tablename, policyname FROM pg_policies WHERE schemaname = 'public' ORDER BY tablename;
     ```
     Xác nhận **tất cả** các bảng nghiệp vụ/hệ thống của app theo prefix dự án (`<prefix>_*`, gồm cả các bảng nền như `var_*` nếu có) đều có ít nhất 1 policy cho `authenticated`. Nếu thiếu, phải tạo ngay:
     ```sql
     CREATE POLICY "authenticated_all" ON public.<tên_bảng> FOR ALL TO authenticated USING (true) WITH CHECK (true);
     ```
     Lưu ý: Supabase bật RLS mặc định cho mọi bảng mới. Nếu bảng có RLS nhưng không có policy → mọi INSERT/UPDATE/DELETE sẽ bị chặn hoàn toàn (`violates row-level security policy`).

7. **Thứ Tự Cột Chuẩn Trong Bảng (Standard Column Order)**:
   - Khi thiết kế bảng database mới hoặc cập nhật bảng cũ, bắt buộc sắp xếp các cột theo đúng thứ tự logic sau:
     1. `id` (Khóa chính `int8`)
     2. Tên/Nhãn hiển thị chính (ví dụ: `ten_phong_ban`, `ho_ten`)
     3. Trạng thái hoạt động (`trang_thai`)
     4. Cột phân nhóm/phòng ban/chức vụ (ví dụ: `nhom`, `id_phong_ban`)
     5. Mô tả & Ghi chú (`mo_ta`, `ghi_chu`)
     6. `id_nguoi_tao`
     7. `tg_tao`
     8. `tg_cap_nhat`

---

## 2. Xác Thực & Đồng Bộ Supabase Auth (Quy Tắc Cứng)

1. **Đăng Nhập Bằng Username (Fake Email)**:
   - Hệ thống sử dụng thống nhất `ten_dang_nhap` để đăng nhập.
   - Khi người dùng nhập `admin`, hệ thống sẽ tự động chuyển hóa thành fake email `admin@gmail.com` để gửi lên Supabase Auth.
   - Tài khoản kiểm thử mặc định: `admin` / `5fedu.com`.
   - Ứng dụng không có luồng đăng ký công khai. Luồng tạo tài khoản do admin kiểm soát.

2. **Cơ Chế Đồng Bộ Supabase Auth Cho Nhân Viên**:
   - **Khi Thêm Mới**: Tạo nhân viên phải gọi API đồng bộ `/api/employee-auth-sync` để tự động tạo tài khoản Supabase Auth tương ứng bằng fake email `<ten_dang_nhap>@gmail.com` với mật khẩu mặc định là `123456`.
   - **Khi Chỉnh Sửa**: Cập nhật `ten_dang_nhap` phải đổi email tương ứng trên Auth.
   - **Khi Xóa**: Vô hiệu hóa hoặc xóa tài khoản Supabase Auth tương ứng để ngăn chặn truy cập trái phép.
   - **Bảo Mật Key**: Các tác vụ quản trị user này bắt buộc phải thực hiện ở server-side (Edge Functions, DB Triggers hoặc admin API). Tuyệt đối cấm đưa `service_role_key` của Supabase vào client-side.

3. **Mềm Dẻo Khi Lỗi Đồng Bộ (Graceful Auth Sync Degradation)**:
   - Trên môi trường Production, nếu API đồng bộ Auth trả về lỗi hoặc thiếu biến môi trường cấu hình Admin Key (gây lỗi `Supabase admin env is missing`), tầng service phải catch lỗi, ghi log cảnh báo và tiếp tục thực hiện lưu/chỉnh sửa database chính thành công, không được ném lỗi (throw error) chặn luồng của người dùng.

---

## 3. Thiết Kế Module Dùng Chung Dữ Liệu (Shared Data Modules Pattern)

Áp dụng pattern `04-business-patterns.md` khi một thực thể vừa là hồ sơ gốc vừa có vai trò nghiệp vụ chuyên sâu.

1. **Một nguồn dữ liệu gốc (Single Source of Truth)**: Không tự tách dữ liệu định danh thành nhiều bảng độc lập gây trùng lặp và lệch đồng bộ. Dùng cờ phân loại, bảng liên kết hoặc bảng mở rộng theo spec.
2. **Giao diện chuyên biệt**: Màn hình chuyên sâu được phép lọc dữ liệu và bổ sung tab/lịch sử nghiệp vụ riêng, nhưng vẫn phải tham chiếu hồ sơ gốc.
3. **Xóa mềm vai trò**: Khi xóa vai trò chuyên sâu, chỉ unlink/chuyển cờ/xóa mềm theo spec; không xóa vật lý hồ sơ gốc nếu hồ sơ còn dùng ở nơi khác.

Ví dụ: Nhân viên là hồ sơ gốc; Tài xế là vai trò chuyên sâu có lịch sử chuyến/lương.

---

## 4. Phân Quyền Ứng Dụng (Permissions Model)

- **Nguyên Tắc Thực Thi**: Phân quyền chủ yếu được kiểm soát ở phía client (app-side) thông qua thông tin phòng ban, cấp bậc và quyền của chức vụ đang đăng nhập.
- **Danh sách Quyền chuẩn**: `xem`, `them`, `sua`, `xoa`, `quan_tri`, `kiem_tra`.

### Quy Tắc Phân Quyền Chi Tiết Cho Các Module:

#### 1. Quyền Xem (Read)
- Chức vụ có `cap_bac = 1` -> Được xem toàn bộ dữ liệu.
- Chức vụ có quyền `quan_tri` -> Được xem toàn bộ dữ liệu.
- Chức vụ có quyền `kiem_tra` -> Được xem toàn bộ dữ liệu.
- Chức vụ có quyền `xem` (nhưng không có `quan_tri` hoặc `kiem_tra`):
  - Nếu `cap_bac = 1` -> Được xem toàn bộ dữ liệu.
  - Nếu `cap_bac = 2` -> Chỉ được xem dữ liệu của những người trong cùng phòng ban (`id_phong_ban` của bản ghi trùng với phòng ban của người dùng đăng nhập).
  - **Cấp bậc khác** (`cap_bac` ≠ 1 và ≠ 2, gồm 3/4/…) -> Chỉ được xem phiếu liên quan trực tiếp đến mình qua người tạo, người phụ trách, vai trò chuyên sâu, hoặc liên kết nghiệp vụ đã chốt trong spec. Nếu spec chưa tách filter nhóm riêng cho cấp thấp hơn, dùng phạm vi cá nhân này cho đến khi owner chốt khác.

#### 2. Quyền Thêm (Create)
- Chức vụ có `cap_bac = 1` OR có quyền `quan_tri` OR có quyền `them` -> Được phép tạo mới bản ghi.

#### 3. Quyền Sửa (Update)
- Chức vụ có `cap_bac = 1` -> Được sửa toàn bộ bản ghi.
- Chức vụ có quyền `quan_tri` -> Được sửa toàn bộ bản ghi.
- Chức vụ có quyền `sua` (nhưng không có `quan_tri`):
  - Nếu `cap_bac = 1` -> Được sửa toàn bộ bản ghi.
  - Nếu `cap_bac = 2` -> Được sửa bản ghi của những người cùng phòng ban (`id_phong_ban`), **nhưng chỉ khi bản ghi chưa được Duyệt hoặc Khóa**.
  - **Cấp bậc khác** -> Chỉ được sửa phiếu liên quan trực tiếp đến mình (mình là người tạo hoặc tài xế tương ứng là mình), **nhưng chỉ khi bản ghi chưa được Duyệt hoặc Khóa**.

#### 4. Quyền Kiểm tra / Duyệt (`kiem_tra` → `check` trên UI)
- **Ngoại trừ `cap_bac = 1` (và tài khoản `admin`)**: mọi hành động duyệt phải có tick **`kiem_tra`** trong ma trận `var_phan_quyen` của module tương ứng. **Không** được suy diễn duyệt từ quyền `sua`/`update` hoặc chỉ vì `cap_bac = 2`.
- Có `quan_tri` (`admin` trên ma trận UI) hoặc `Tất cả` → duyệt toàn phạm vi module.
- Chỉ có `kiem_tra` → duyệt trong phạm vi `cap_bac`: cấp 2 cùng phòng ban; cấp ≥3 bản ghi liên quan trực tiếp (`id_nguoi_tao` / `id_tai_xe` / chính mình).

#### 5. Quy tắc Ma trận bắt buộc (Owner feedback 2026-06-13)
- **Ngoại trừ `cap_bac = 1`**: mọi quyền xem/thêm/sửa/xóa/duyệt phải đối chiếu **bảng ma trận** `var_phan_quyen` đã hydrate (`matrixActive === true`). Không tự cấp quyền ngầm theo cấp bậc nếu ma trận chưa tick.
- `cap_bac` chỉ quyết định **phạm vi dòng** sau khi đã có grant tương ứng trong ma trận (trừ cấp 1 toàn quyền).

#### 6. Quyền Xóa (Delete)
- Chức vụ có `cap_bac = 1` -> Được xóa toàn bộ bản ghi.
- Chức vụ có quyền `quan_tri` -> Được xóa toàn bộ bản ghi.
- Chức vụ có quyền `xoa` (nhưng không có `quan_tri`):
  - Nếu `cap_bac = 1` -> Được xóa toàn bộ bản ghi.
  - Nếu `cap_bac = 2` -> Được xóa bản ghi của những người cùng phòng ban (`id_phong_ban`), **nhưng chỉ khi bản ghi chưa được Duyệt hoặc Khóa**.
  - **Cấp bậc khác** -> Chỉ được xóa phiếu liên quan trực tiếp đến mình (mình là người tạo hoặc tài xế tương ứng là mình), **nhưng chỉ khi bản ghi chưa được Duyệt hoặc Khóa**.

#### 7. Toolbar Actions Vs Nội Dung Tính Toán
- Toolbar/list/detail action phải theo quyền hành động của user, không bị ẩn chỉ vì dữ liệu nguồn tính toán hiện chưa đủ điều kiện.
- Nội dung render, preview, in/export và total phải chỉ lấy các dòng đủ điều kiện nghiệp vụ theo spec.
- Danh sách phải hiện mọi bản ghi trong phạm vi quyền xem; số tiền/tổng/trạng thái tổng hợp vẫn phải tính từ source records hợp lệ.

Ví dụ: phiếu lương có thể hiện action in/chi tiết theo quyền, nhưng tổng lương chỉ tính từ dòng công/chuyến đủ điều kiện.

#### 8. Định dạng Module Key (`id_module`)
- **Quy tắc**: Cột `id_module` trong bảng `var_phan_quyen` bắt buộc lưu dưới dạng slug tiếng Việt không dấu (ví dụ: `nhan-vien`, `chuyen-xe`, `bang-luong`, `tai-xe`), tuyệt đối cấm sử dụng đường dẫn phân cấp (như `he-thong/nhan-vien` hoặc `quan-ly-van-tai/chuyen-xe`).

#### 9. Quy tắc Lưu Trữ Quyền "Tất cả" (UI vs Database)
- **Quy tắc**: Giao diện (Frontend) được phép có checkbox "Tất cả" để tự động chọn tất cả quyền khi click.
- **Hành vi lưu DB**: Khi lưu xuống bảng `var_phan_quyen`, hệ thống **không được lưu** dòng dữ liệu có giá trị `quyen = 'Tất cả'` hay `tat-ca`. Phải tách và lưu thành các dòng quyền riêng lẻ tương ứng (`xem`, `them`, `sua`, `xoa`, `quan_tri`, `kiem_tra`).

#### 10. Quy chuẩn Trạng thái Khóa, Duyệt Và Thực Hiện
- Áp dụng `04-business-patterns.md` cho các nghiệp vụ có hai trục trạng thái: **thực hiện** và **phê duyệt**.
- Một bản ghi được coi là khóa khi trạng thái duyệt/khóa theo spec đã đạt ngưỡng không cho sửa. UI và service đều phải khóa hành động sửa/xóa cho user không đủ quyền.
- Với mô hình cha-con, phải xác định rõ trạng thái duyệt của cha cascade xuống con hay aggregate từ con lên cha.
- Trạng thái thực hiện của dòng con được phép độc lập với trạng thái duyệt nếu spec có hai trục này.
- Total/rollup/report/export chỉ tính các dòng thỏa đủ điều kiện nghiệp vụ đã chốt.

Ví dụ: phiếu cha có nhiều dòng con; dòng con đã thực hiện nhưng chưa duyệt thì được tính tiến độ thực hiện, nhưng chưa được tính vào total đã duyệt nếu spec yêu cầu đủ cả hai điều kiện.

---

## 5. Phòng Ngừa Lỗi Phát Sinh Chéo & Bảo Vệ Truy Vấn Dữ Liệu (Regression Protection)

Để giảm thiểu tối đa hiện tượng "sửa chỗ này hỏng chỗ kia" và bảo đảm tính ổn định của các truy vấn dữ liệu:

1. **Phân Tích Tác Động Tĩnh (Dependency Check)**:
   - Trước khi thay đổi bất kỳ trường cơ sở dữ liệu nào, sửa API endpoint, hoặc cấu trúc dữ liệu truyền nhận, AI bắt buộc phải chạy `grep_search` trên toàn dự án để tìm tất cả các vị trí đang gọi/tham chiếu đến thực thể đó và cập nhật đồng loạt.

2. **Đối Chiếu Schema Trực Tiếp (Remote DB Verification)**:
   - Xác minh cấu trúc thực tế của bảng bằng migration/source đáng tin, CLI/query DB hoặc SQL Editor khi có quyền; tuyệt đối cấm đoán mò hoặc bịa cấu trúc DB.
   - Với Codex, không dùng browser/Supabase Dashboard mặc định. Chỉ mở browser khi user yêu cầu rõ hoặc không có kênh query/source nào đủ xác minh; nếu thiếu quyền DB và browser là kênh duy nhất còn lại, báo `PARTIAL` với gap đó.

3. **Hủy Cache Đồng Bộ (Query Cache Invalidation)**:
   - Khi thực hiện các hành động CRUD (Thêm, Sửa, Xóa) dữ liệu, AI bắt buộc phải invalidate cache tương ứng ở phía Frontend (ví dụ: gọi `queryClient.invalidateQueries(['key-dung-chung'])` hoặc trigger invalidate theo cache keys) để đồng bộ trạng thái mới ngay lập tức, tránh lỗi dữ liệu cũ đè lên dữ liệu mới.

4. **Kiểm Tra Build & E2E**:
   - Sau khi chỉnh sửa, AI phải tự chạy thử lệnh build (`npm run build`) cục bộ hoặc check tương đương.
   - Với Codex, ưu tiên unit/integration tests, service/API checks, source trace, DB query và artifact inspection. Browser/Playwright production chỉ chạy khi user yêu cầu rõ; nếu không chạy browser, báo evidence thay thế và gap còn lại nếu có.
   - Thay đổi module có master-detail, approval, cascade, rollup hoặc derived totals: chạy unit/E2E trong blast radius tương ứng nếu repo có harness. Với project transport legacy, chạy trip execution specs nếu các spec đó tồn tại; mutating test phải restore fixture.
## Database, Permission And Cross-Data Verification Gate

Khi task đụng database, auth, permission, role, row-level filtering, trigger, rollup hoặc service ghi dữ liệu:

- Đối chiếu schema thật bằng query hoặc migration/source đáng tin cậy trước khi sửa.
- Sau CRUD, query lại database để xác nhận record, field, audit column, foreign key và dữ liệu liên quan đúng.
- Với trigger/rollup/cascade, kiểm tra bảng cha, bảng con, báo cáo và module liên quan sau `INSERT`, `UPDATE`, `DELETE`.
- Với permission, tạo hoặc dùng đủ account đại diện các cấp quyền. Mỗi account phải test read/list/detail và full action được phép/không được phép trong scope.
- Test truy cập trái phép qua URL/API nếu hệ thống có bề mặt đó.
- Không chỉ test admin.
- Nếu external integration không thể test thật, đọc code/config/error handling kỹ và ghi rõ verification gap.

## 5fedu-Specific Baseline
 
- App table primary key mặc định là `id int8` auto-increment.
- Foreign key tới app table cũng dùng `int8`.
- Login dùng `ten_dang_nhap`; admin mặc định `admin` / `5fedu.com`.
- Tài khoản thường mặc định `123456`.
- Supabase service role chỉ được dùng ở server/admin path, không nằm ở client.
- Dữ liệu tự tính phải nhất quán ở database khi feature/báo cáo/export phụ thuộc vào nó.
- **Quy tắc nghiệp vụ phân duyệt đa cấp**: Đối với các nghiệp vụ có phân cấp kiểm duyệt (phê duyệt đa cấp), cấu trúc cơ sở dữ liệu phải hỗ trợ tách biệt rõ ràng hai trục trạng thái: Trạng thái thực hiện (ở mức phiếu chi tiết dòng con) và Trạng thái phê duyệt (thường ở mức phiếu cha). Khi cập nhật trạng thái duyệt ở bảng cha thành `Đã duyệt` hoặc `Không duyệt`, hệ thống phải cascade (đổ) trạng thái này xuống các dòng con tương ứng và khóa toàn bộ quyền chỉnh sửa của người dùng.
