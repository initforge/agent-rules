# trigger-audit.json

**Role:** CI / fixture only — recall checks for skill `description` keywords and domain files.

**Not** a runtime router. Runtime trigger SoT = each skill `description` frontmatter (`rules/30-context-routing.md`).

Do not teach agents to consult this file mid-task for routing decisions.
