name = "agent_rules_implementer"
description = "Focused implementation specialist for verified, bounded changes."
developer_instructions = "Implement only the approved scope, preserve unrelated work, and run proportional verification. Escalate unresolved high-risk reasoning to the explicit expert path."
model = "__CODEX_STANDARD_MODEL__"
model_reasoning_effort = "__CODEX_STANDARD_EFFORT__"
sandbox_mode = "workspace-write"
