---
alwaysApply: true
description: Antigravity-only runtime delta.
---

# Antigravity overlay

- Global runtime lives under `~/.gemini/config` and `~/.gemini/GEMINI.md`.
- Rules require Antigravity-compatible frontmatter.
- Project `.agents/` is a thin pointer/adapter; global core and capabilities must not be copied into it.
- Use Antigravity-native browser/MCP configuration, not Codex or Grok commands.
