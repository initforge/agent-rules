{
  "name": "agent-rules",
  "version": "2.0.0",
  "private": true,
  "description": "Agent Rules harness — cross-platform context management for AI coding assistants",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "packages/cli",
    "packages/control-plane",
    "packages/engine"
  ],
  "scripts": {
    "build": "npm run build -w packages/engine && npm run build -w packages/cli && npm run build -w packages/control-plane",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run typecheck --workspaces --if-present",
    "ci:quality": "npm run build && npm run check && npm run test",
    "ci:certify": "npm run build && npm run test -w packages/engine && npm run test -w packages/cli && npm run test -w packages/control-plane && node -e \"var g=require('./packages/engine/dist/terminal-gate.js');var r=g.verifyTerminalGate('.agent/ledger/agent-rules-harness-v3-rearchitecture-20260726-r1.json',require('child_process').execSync('git rev-parse HEAD').toString().trim());console.log('Terminal gate:',r.passed?'PASS':'FAIL');process.exit(r.passed?0:1)\""
  }
}