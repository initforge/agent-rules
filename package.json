{
  "name": "agent-rules",
  "version": "2.0.0",
  "private": true,
  "description": "Agent Rules harness — cross-platform context management for AI coding assistants",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "packages/cli",
    "packages/kernel"
  ],
  "scripts": {
    "build": "npm run build -w packages/kernel && node automation/build-runtime.mjs && npm run build -w packages/cli",
    "test": "node node_modules/vitest/vitest.mjs run --config vitest.config.ts && npm run test --workspaces --if-present",
    "test:fast": "npm test",
    "check": "npm run typecheck --workspaces --if-present",
    "ci:quality": "npm run build && npm run check && npm run test",
    "verify:all": "node automation/verify-all.mjs",
    "verify:ci": "node automation/verify-all.mjs"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "^1.18.10",
    "tsx": "^4.23.12"
  }
}
