{
  "name": "agent-rules",
  "version": "2.0.0",
  "private": true,
  "description": "Agent Rules harness — cross-platform context management for AI coding assistants",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "packages/cli",
    "packages/control-plane",
    "packages/engine"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run typecheck --workspaces --if-present",
    "ci:quality": "npm run build --workspaces 2>/dev/null || cd packages/engine && npx --no-install tsc && cd ../cli && npx vitest run && cd ../control-plane && npx vitest run && cd / && echo QUALITY_DONE",
    "ci:certify": "npm run build --workspaces 2>/dev/null || cd packages/engine && npx vitest run && cd ../cli && npx vitest run && cd ../control-plane && npx vitest run && cd / && echo CERTIFY_DONE"
  }
}