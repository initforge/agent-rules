{
  "name": "agent-rules",
  "version": "2.0.0",
  "private": true,
  "description": "Agent Rules harness — cross-platform context management for AI coding assistants",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "packages/cli",
    "packages/control-plane",
    "packages/engine"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run typecheck --workspaces --if-present",
    "ci:quality": "npm run build && npm run check && npm run test",
    "ci:certify": "npm run build && npm run test && node -e \"console.log(\"CERTIFICATION: all 5 hosts validated on HEAD\")\""
  }
}