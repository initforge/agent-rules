interface:
  display_name: "Researcher"
  short_description: "Structured local and external research"
  default_prompt: "Use $researcher to gather evidence, compare options, and write a research note before coding."

policy:
  allow_implicit_invocation: true
