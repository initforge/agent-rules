# context-evolution-protocol Skill

0. Turn-0: `Skill scan:` → context/rule/skill/workflow/harness learning signal → **before editing context**.
1. Read `.agents/skills/context-evolution-protocol/SKILL.md`.
2. Classify the learning item: one-off, project-specific, 5fedu-common, global-agent-rule, raw-evidence, or question.
3. Run duplicate/layer audit with targeted search before adding or rewriting context.
4. Promote only generalized rules into living context; keep raw evidence out of default-loaded files.
5. Sync source, mirrors, global runtime, and relevant project copies.
6. Verify validators/preflight and report Classification, Layer changed, Duplicate audit, Verification, and Status PASS/PARTIAL/BLOCKED.
