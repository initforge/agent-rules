# playwright Skill (CLI debug)

0. Turn-0: snapshot/debug 1 màn → `playwright`; suite → redirect `e2e-qa`.
1. Read `.agents/skills/playwright/SKILL.md`.
2. Use playwright-cli; escalate to `e2e-qa` for specs/matrix.