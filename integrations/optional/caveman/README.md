# Caveman integration

Caveman is an optional upstream dependency, not canonical knowledge. This folder stores only install ownership and lifecycle wrappers. It never vendors upstream `SKILL.md`, hooks or Python payload.

`install.ps1` is dry-run by default. Pass `-Apply` to mutate the selected platform.
