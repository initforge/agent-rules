{
  "mcp": {
    "docker": {
      "type": "local",
      "command": [
        "docker",
        "run",
        "-i",
        "--rm",
        "-v",
        "//var/run/docker.sock:/var/run/docker.sock",
        "mcp/docker"
      ]
    }
  }
}
