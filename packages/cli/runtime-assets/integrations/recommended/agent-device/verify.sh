#!/usr/bin/env bash
set -euo pipefail
# agent-device verify script (recipe; run only under install authority)
echo "[recipe] agent-device verify: agent-device --version && agent-device help workflow"
