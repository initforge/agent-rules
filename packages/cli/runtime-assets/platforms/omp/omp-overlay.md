# OMP overlay

OMP's native owned surface is the active agent directory: `AGENTS.md`,
`skills/<name>/SKILL.md`, and optional `mcp.json`. This file documents that
projection only; imported third-party settings are never used as OMP proof.
