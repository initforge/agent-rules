# Maintenance and risks

Run context, reference, mirror and integration validation before installing global runtimes. Keep platform overlays small and never put domain procedures in always-loaded core.

External integrations require pinned versions and integrity checks. Generated project indexes remain local unless a project explicitly chooses a shared artifact policy. Preserve unrelated dirty-worktree changes and never commit/push automatically.
