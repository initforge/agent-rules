# OpenCode AM-0022 running-session steers

These are delta prompts for sessions already in progress. They reference, rather than duplicate, the canonical launch handoff. The previously delivered script-first/conditional-Compose/full-local-simulation directive remains effective; Docker is never introduced when the declared project stack does not require it.

## `agent-rules`

```text
Tiếp tục từ filesystem truth hiện tại; không restart, discard hoặc làm lại discovery. Đọc lại toàn bộ P:\agent-rules\.agent\handoffs\agent-rules-harness-v3-rearchitecture-20260726-r1\opencode-am0022-execution-handoff.md và áp dụng ngay Mandatory execution invariants. Giữ nguyên directive script-first/conditional-Compose/full-local-sim đã nhận trước đó; không thêm Docker nếu declared stack không cần.

Đưa post-AM-0022 owner delta vào canonical additive amendment/CAS như lifecycle slice đầu nhưng không chặn READY work khác. Điều chỉnh active pool về 8 normal/10 burst, tối thiểu 6 trừ blocker có bằng chứng; main chỉ điều phối bằng bounded receipts. Tiếp quản known gaps, rolling review/watchdog/integration và chạy đến installed enforcement cùng truthful terminal proof; không hỏi tiếp tục.

Observed host gap: installed OpenCode supervisor vẫn batch tối đa 4, request timeout 10 phút và không có semantic watchdog thực thi; engine watchdog hiện chưa có production caller. Sửa canonical config/adapter/build/install để native supervisor tự diagnose ở soft stall, checkpoint + exact-child cancel/reassign ở hard stall; không sửa tay runtime mirror và không coi prompt-only là enforcement.

Harness phải sở hữu canonical host-kit gồm portable role/capability contracts, model-neutral agent-role `.md`, permissions, scheduler/watchdog/process/test governors, debug/doctor, installer và conformance. Adapter chỉ generate native config/agent files cho từng platform; model/provider mapping nằm trong adapter-owned catalog và runtime attestation, không hardcode trong prompt/role semantics. Doctor phải thấy loaded config hash, role/permission, semantic cursor/deadline, queued-message age, child session, PID/process group, port/test/MCP/browser/Compose ownership và orphan candidates; recovery exact-scoped, journaled, idempotent.

Đọc `P:\agent-rules\.agent\research\opencode-stuck-failure-modes-20260803.md`, đưa OC-STUCK-01..08 vào additive amendment/CAS và implement thành installed conformance, không chỉ prompt. Bắt buộc proof: permission/headless không chờ ẩn, child không spawn child, child crash/cancel vẫn có handle và parent reconcile được, event/session mismatch không treo, provider/tool/MCP hung được bounded recovery, Windows cancel/crash không còn owned orphan; chạy fault injection trên fresh process rồi mới PASS enforcement.

Áp dụng owner delta `HIGH-ENDURANCE-STEP-BUDGET` trong execution handoff qua amendment kế tiếp sau khi xử lý trạng thái AM-0023 hiện tại. Generate role steps từ canonical host-kit, build/install và fresh-process attest; không sửa tay runtime mirror. Step-limit phải checkpoint ở 75%, tự continuation khi còn semantic progress và tự split/đổi strategy nếu lặp lại; tuyệt đối không dừng project, mất work hoặc làm yếu watchdog.
```

## `pos-ops`

```text
Tiếp tục từ filesystem truth và dirty candidate hiện tại; không restart, discard hoặc làm lại discovery. Đọc lại toàn bộ P:\agent-rules\.agent\handoffs\agent-rules-harness-v3-rearchitecture-20260726-r1\opencode-am0022-execution-handoff.md và áp dụng Mandatory execution invariants như additive project overlay. Giữ nguyên directive script-first/conditional-Compose/full-local-sim đã nhận trước đó; không thêm Docker nếu declared stack không cần.

Freeze candidate epoch nếu chưa có, dispatch full-convergence READY wave ngay với 8 normal/10 burst và tối thiểu 6 trừ blocker có bằng chứng. Main chỉ điều phối; dùng bounded receipts, rolling risk review, watchdog và reviewed integration train. Không chờ agent-rules hoàn tất, không dừng ở lỗi repairable; kết thúc bằng project proof, behavioral dogfood và enforcement status tách biệt.

Áp dụng guard tạm thời đến khi host-kit được cài: main ghi child handle/ownership trước khi chờ; child cấm spawn child; autonomous role không được có `ask`/`question` ẩn; mọi provider/tool/MCP/process wait có deadline và semantic-progress checkpoint; cancel/cleanup đúng owned process group. Nếu runtime chưa enforce thì ghi `PROMPT_ENFORCED`, không báo engine PASS và vẫn tiếp tục READY work khác.
```

## `mini-toeic.score`

```text
Tiếp tục từ filesystem truth và dirty candidate hiện tại; không restart, discard hoặc làm lại discovery. Đọc lại toàn bộ P:\agent-rules\.agent\handoffs\agent-rules-harness-v3-rearchitecture-20260726-r1\opencode-am0022-execution-handoff.md và áp dụng Mandatory execution invariants như additive project overlay. Giữ nguyên directive script-first/conditional-Compose/full-local-sim đã nhận trước đó; không thêm Docker nếu declared stack không cần.

Freeze candidate epoch nếu chưa có và tiếp tục GAP-CLOSE/MINIMAL-INTERVENTION, không broad refactor hay mở lại behavior đã accepted nếu không có bằng chứng. Dispatch 8 normal/10 burst, tối thiểu 6 trừ blocker có bằng chứng; main chỉ điều phối bằng bounded receipts. Rolling review/watchdog/integration đến khi đóng mọi gap bằng fresh proof và báo riêng minimal dogfood delta với enforcement status.

Observed recovery gap: Mini dùng Jest nhưng một worker đã gọi trực tiếp Vitest và tạo nhiều worker con; cấm lặp lại, dùng native Jest focused proof với `--runInBand`. API local PID được tạo như disposable nhưng parent đã chết; xác minh ownership của browser claim, rồi cleanup đúng PID/process group khi claim kết thúc và luôn dùng try/finally hoặc owned process supervisor để không còn orphan.

Áp dụng guard tạm thời đến khi host-kit được cài: main ghi child handle/ownership trước khi chờ; child cấm spawn child; autonomous role không được có `ask`/`question` ẩn; provider/tool/MCP/process wait phải bounded theo semantic progress; parent phải reconcile child crash/cancel thay vì tin spinner. Ghi `PROMPT_ENFORCED` nếu chưa có installed proof và không để nghi thức này mở rộng scope GAP-CLOSE.
```
