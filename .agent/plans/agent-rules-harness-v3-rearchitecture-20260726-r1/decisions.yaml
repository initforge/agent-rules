schema_version: 1
recorded_reversible_defaults:
  - decision_id: D-001
    domain: install
    default: allow
    reason: reversible via rollback
    requires_owner: false
  - decision_id: D-002
    domain: test-rerun
    default: allow
    reason: read-only
    requires_owner: false
  - decision_id: D-003
    domain: worktree-create
    default: allow
    reason: isolated, reclaimable
    requires_owner: false
unknowns_register:
  - unknown_id: U-001
    kind: EXTERNAL
    subject: native host observed-model attestation
    wake: provider health/CI watcher
    state: WAITING_EXTERNAL
  - unknown_id: U-002
    kind: EXTERNAL
    subject: external CI green on current HEAD
    wake: CI watcher
    state: WAITING_EXTERNAL
clarification_batch:
  policy: owner questions batched once before execution; allowed only for product intent, unavailable credentials, destructive data changes, or new irreversible authority
  pending: []
  source: AM-0019 §3
