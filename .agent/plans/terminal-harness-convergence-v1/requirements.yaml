version: 1
plan_id: terminal-harness-convergence-v1
frozen_contract: .agent/plans/terminal-harness-convergence-v1/plan.md
mapping_source: plan section 6 (13-domain) and section 7 (PF1-6)

requirements:
  - id: REQ-001
    source_id: A
    statement: "One terminal authority: Evidence Ledger, acceptance audit, convergence and Closure Service reducers are merged into a single canonical authority; old reducers are deleted or delegated, never kept with independent semantics."
    phases: [P1, P7]
    status: pass
  - id: REQ-002
    source_id: A
    statement: "TrustedTerminalDecision is the single type consumed by north-star run, queue runner, close, closeout, certify, release automation and the result renderer; exit 0 and DONE/completed are produced only from PASS."
    phases: [P1]
    status: pass
  - id: REQ-003
    source_id: A
    statement: "Fail-closed ordering FAILED -> NEEDS_USER -> BLOCKED -> UNSUPPORTED -> PARTIAL -> PASS is enforced everywhere; PARTIAL/FAILED/BLOCKED/NEEDS_USER/UNSUPPORTED never exit success or render completed."
    phases: [P1, P6]
    status: pass
  - id: REQ-004
    source_id: D
    statement: "Closure composes candidate evaluation -> stage -> atomic commit -> external attestation when required -> CAS deactivate -> compact/archive -> final decision; closeout/certify are compatibility entrypoints calling the canonical service."
    phases: [P1]
    status: pass
  - id: REQ-005
    source_id: D
    statement: "close loads real requirements/evidence/reconciliation/proof identities; it does not synthesize evidence_status=pending and does not hard-code corrections for an old plan; candidate with sufficient evidence but no attestation is PARTIAL with attestation_eligible=true."
    phases: [P1]
    status: pass
  - id: REQ-006
    source_id: D
    statement: "Closure transaction is cross-platform: uses secure-fs helper instead of openSync(dir,'r+'); replay binds hash of full input, evidence and the five identities; tests cover stage/commit/correction/crash/replay/input-drift on Linux/macOS/Windows."
    phases: [P1]
    status: pass
  - id: REQ-007
    source_id: K
    statement: "Every proof/closure/install receipt binds the five identities (harness release, installed projection, consumer repository, consumer candidate, host runtime); cross-repo/cross-package/stale-projection/stale-candidate replay is rejected."
    phases: [P1, P6]
    status: pass
  - id: REQ-008
    source_id: B
    statement: "Every load-bearing defect has a causal map: symptom -> canonical authority -> host-neutral semantics -> host mapping -> install/migration -> consumer observation -> proof; no named-repo special case accepts a project-local patch as global remediation."
    phases: [P0, P2, P8]
    status: pass
  - id: REQ-009
    source_id: H
    statement: "compileDoD() no longer derives depth-of-done from disposition/executor; handoff always writes EXPORT_HANDOFF with raw intent/hash, causal map, scope/invariants, forbidden effects, CompiledDoD, proof/live/install/cleanup obligations, repair budget and stop conditions."
    phases: [P2]
    status: pass
  - id: REQ-010
    source_id: C
    statement: "Generic empty-context worker completes (or returns a precise blocker) using only the frozen one-copy artifact; no research redo; early-stop fixture where worker stops after code/test but lacks install/live/closure fails."
    phases: [P2, P6]
    status: pass
  - id: REQ-011
    source_id: E
    statement: "Decision Fabric is the active default; legacy routeSkills() only supplies candidate hints and must pass through typed RepoFacts/TaskFacts/policy; multilingual intent yields semantically equivalent routing; phrase cannot self-grant dangerous/domain authority."
    phases: [P3, P6]
    status: pass
  - id: REQ-012
    source_id: D
    statement: "Proof Router is split into planProofRoute() (before execution) and completeProofRoute() (after); runtime/resume/proof-plan/handoff/provider flows all use the same route plan; only selected verifiers run; omitted proofs carry reason + invalidation condition."
    phases: [P3]
    status: pass
  - id: REQ-013
    source_id: I
    statement: "Host capability contracts refreshed from current official docs with doc version/access date, native capability, fallback enforcement and live evidence; enforcement order native -> broker -> isolated worktree -> BLOCKED; absent binaries stay static/NOT_LIVE_VERIFIED."
    phases: [P4]
    status: pass
  - id: REQ-014
    source_id: I
    statement: "Runtime installer proves adapter -> projection -> package hash -> install/upgrade -> isolated host home -> generic consumer session; G2 preserves project-owned config and surfaces (not overwrites) conflict with hard policy; secret/path/effect policy applies to .env/credentials/host config/MCP output/logs."
    phases: [P4, P6]
    status: pass
  - id: REQ-015
    source_id: L
    statement: "Artifact admission is wired into operational-write entrypoints (runtime/planner/handoff/evidence); EPHEMERAL keeps minimal durable terminal evidence; operational state lives outside tracked consumer source or under ignored harness state; fresh repo leaves no file forest."
    phases: [P5, P7]
    status: pass
  - id: REQ-016
    source_id: J
    statement: "Lane controller with acquire/release in finally, bounded queue and cancellation connects read_search/research/writer/browser/verifier/mcp/heavy_process to runner/verifier/browser/MCP/build; memory pressure shrinks expensive lanes first; Windows unknown load is not treated as idle."
    phases: [P5, P6]
    status: pass
  - id: REQ-017
    source_id: G
    statement: "MCP idle-zero: no harness-owned process/socket/lease/advertised provider/orphan/schema exposure after no-MCP task; receipt schema/predicate share semantics; failed cleanup receipt fails closed; CPU/RSS recorded only with PID attribution else NOT_APPLICABLE."
    phases: [P5, P6]
    status: pass
  - id: REQ-018
    source_id: M
    statement: "G1-G4 generic fixtures + adversarial/composition tests are mandatory release gates with oracles against false DONE/replay/stale identity; RAW HOST vs current vs candidate ablation runs on generic corpus; mechanisms winning only in dogfood are retired."
    phases: [P6]
    status: pass
  - id: REQ-019
    source_id: PF6
    statement: "Release completion: exact package built, installed into isolated host homes, used on generic fixtures, exact-SHA CI green, terminal closure idempotent and atomically deactivated."
    phases: [P6, P8]
    status: pass
  - id: REQ-020
    source_id: A
    statement: "Simplification: duplicate terminal reducers, shadow skill authority, direct-write bypass and dead compatibility code removed after parity; generated/ regenerated by canonical automation; no requirement flipped to PASS by prose."
    phases: [P7]
    status: pass
  - id: REQ-021
    source_id: PF6
    statement: "History convergence: deterministic git commit-tree rewrite to default 7 (<=9) commits with final tree == verified tree; safe force-with-lease push to remote main with exact expected SHA; exact-SHA Quality/Certification terminal and green; two historical tags retained."
    phases: [P8]
    status: pass
