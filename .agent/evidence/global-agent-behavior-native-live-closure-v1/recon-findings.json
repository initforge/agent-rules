{
  "schema": "agent-rules/recon-findings",
  "version": 1,
  "plan_id": "global-agent-behavior-native-live-closure-v1",
  "captured_at": "2026-08-25T00:00:00Z",
  "subagents": [
    {
      "id": "kernel-module-overlap",
      "finding": "68 northstar files; runtime.ts derives trusted_outcome itself (lines 705/794) and writes .agent/runs directly via writeJsonAtomic (~40 calls) bypassing RunStore; parallel outcome reducers in closure-gates.evaluateNorthStarClosure, closure-service.deriveOutcome, portable-plan.auditPreHandoff, receipts; duplicate names decision-fabric/decision-closure/planner-runtime/closure-*/context-*; RunStore unused by kernel runtime."
    },
    {
      "id": "cli-rule-residue",
      "finding": "Exactly 8 top-level commands registered in index.ts; 33 dead command modules on disk + ~15 tests exercising them; integration command still accepts legacy aliases install/verify/uninstall (integration.ts:6,32,35; index.ts:177); vibe-coder/plain-vietnamese/technical_explain zero occurrences; operator-profile only in 2 eval fixtures; rules/manifest.yaml clean at 5 but many automation/docs/skills still reference archived legacy rule files (00-bootstrap, 10-execution, 25-task-lifecycle, 50-context-budget, 45-adaptive-minimal-proof-testing...); validate-proof-mirror-parity.mjs requires archived 45 rule in mirrors (conflict)."
    },
    {
      "id": "host-platforms",
      "finding": "registry has all 8 hosts with full NativeHostContract fields; platforms/* all have adapter.ts + overlay; 10-method HostAdapter contract does NOT exist (reload/readback/offlineCanary/authenticatedCanary missing); NativeInstaller.certify() fills claims from contract strings and hardcodes MODEL_BEHAVIOR=NEEDS_USER for all hosts; Antigravity logged-in (hook-health NATIVE_OBSERVED live event 2026-08-25T09:43Z, only host) => model-turn live required; cursor/grok signed-out => MODEL_BEHAVIOR=NEEDS_USER; cursor has auth contradiction (AUTHENTICATED_LOCAL in verify-windows vs UNAUTHENTICATED install receipt); two installers + one reconciler + legacy PlatformAdapter stubs."
    },
    {
      "id": "skills-routing",
      "finding": "exactly 34 global skills in skills/ (5fedu pair under profiles/5fedu/skills, scope profile); all 34 have SKILL.md + ROUTE.json; SKILL.md has signals/excludes metadata with source:'ROUTE.json migrated'; ROUTE.json still the active routing authority in context-graph.ts loadSkillRouting (prefers ROUTE.json), routing.ts assertSelectedSourceIntegrity hashes routing_source=ROUTE.json, generated context-graph.json records routing_source=.../ROUTE.json; catalog.json source_of_truth still ROUTE.json shape; one resolver implementation (routeSkills) with two data paths (graph + hard-coded FALLBACK_SIGNALS regex); validate-external-sources.mjs requires sidecar==='ROUTE.json'; no live canaries found for the 7 workflow skills."
    }
  ],
  "action_items": [
    "Wire RunStore as single writer in runtime.ts; make OutcomeReducer single reducer; consolidate duplicate names",
    "Restrict integration to list|enable|disable|doctor; remove dead command modules/tests; update docs referencing non-8 commands",
    "Update automation/docs/skills referencing archived legacy rule files; fix validate-proof-mirror-parity conflict",
    "Implement 10-method HostAdapter with real reload/readback/offlineCanary/authenticatedCanary; stop claim-filling certify(); resolve cursor auth contradiction",
    "Flip routing source to SKILL.md metadata; update catalog source_of_truth; remove FALLBACK_SIGNALS second path; add per-skill pos/neg tests + 7 live canaries"
  ]
}