# LIVE STEER — DOGFOOD LATEST AGENT-RULES CONCEPTS IN THE CURRENT PROJECT

Nói và báo cáo bằng tiếng Việt. Apply this prompt from the current project root.
This prompt is intended for the active `pos-ops` or `mini-toeic.score` session.
Resume from current filesystem, Git, project `.agent` artifacts, child receipts
and running process truth. Do not restart, reset, clean, discard dirty work or
rewrite the project's immutable original plan.

## 1. Load stable core plus immutable handoff

The installed runtime at `/home/linhnx/.codex/agent-rules-runtime` is a usable
stable core but is not the fully installed AM-0019/0020/0021 product.

Read and verify completely:

```text
/home/linhnx/Projects/agent-rules/.agent/handoffs/agent-rules-harness-v3-rearchitecture-20260726-r1/latest-concepts-dogfood-manifest.json
/home/linhnx/Projects/agent-rules/.agent/handoffs/agent-rules-harness-v3-rearchitecture-20260726-r1/cross-project-dogfood/v1/manifest.json
/home/linhnx/Projects/agent-rules/.agent/handoffs/agent-rules-harness-v3-rearchitecture-20260726-r1/cross-project-dogfood/v1/adoption-protocol.md
/home/linhnx/Projects/agent-rules/.agent/plans/agent-rules-harness-v3-rearchitecture-20260726-r1/amendments/0019-autonomous-native-swarm-whole-system-convergence.md
/home/linhnx/Projects/agent-rules/.agent/plans/agent-rules-harness-v3-rearchitecture-20260726-r1/amendments/0020-epistemic-integrity-adversarial-review-and-truthful-reporting.md
/home/linhnx/Projects/agent-rules/.agent/plans/agent-rules-harness-v3-rearchitecture-20260726-r1/amendments/0021-premium-main-context-economy-and-event-driven-orchestration.md
```

Expected hashes:

```text
AM-0019  074eda76c69d91b91bf25ab74888dc7ada13376cbfd29d0e67e4a8c6f8662a21
AM-0020  f99e603c2e9c60194518938f78de4ab90645eb02d893f2ef811b436c444ee0cc
AM-0021  0dfb45500fe8a7d80f177e57ef8a6c231b44e28f8e4f973b31f85bf7d527cf1c
```

Do not copy the agent-rules original plan over this project's plan. Instead,
record one additive project-local owner amendment/decision that adopts only the
operational behaviors below. Preserve the project's existing ledger format and
use its canonical engine/API when available. Mark enforcement honestly as:

```text
BEHAVIORAL_DOGFOOD_PENDING_FULL_HARNESS_INSTALL
```

Do not claim engine enforcement that does not yet exist in the installed core.

Select and verify the matching project binding:

```text
/home/linhnx/Projects/agent-rules/.agent/handoffs/agent-rules-harness-v3-rearchitecture-20260726-r1/cross-project-dogfood/v1/bindings/pos-ops.json
/home/linhnx/Projects/agent-rules/.agent/handoffs/agent-rules-harness-v3-rearchitecture-20260726-r1/cross-project-dogfood/v1/bindings/mini-toeic.score.json
```

The binding is an owner-capture checkpoint, not an instruction to roll back.
If current state advanced, recapture a new candidate fingerprint while
preserving the bound checkpoint as history.

## 2. Freeze the real dirty candidate before fan-out

The current project contains substantial tracked and untracked work that is not
present in Git HEAD. Ordinary worktrees from HEAD would use the wrong candidate.

Before new writers start:

1. Pause new mutations only long enough to fingerprint the current candidate.
2. Record HEAD/tree, tracked/index diff hashes, allowlisted untracked-source
   manifest, original-plan hash and legacy-ledger hash.
3. Secret-scan untracked/build-critical files; exclude credentials, databases,
   caches and runtime output from snapshots.
4. Prefer a reversible local checkpoint commit on a private integration branch
   when existing authority permits local commits; never push it solely for
   dogfood adoption.
5. Otherwise materialize a content-addressed patch plus allowlisted untracked
   source archive into isolated copies. Do not branch from stale HEAD.
6. Bind every worker, verifier and reviewer to the same candidate epoch.
7. Freeze the schema-v4 ledger as `LEGACY_CANDIDATE`; do not treat its
   self-claims or prior summaries as terminal truth.

## 3. Operational behaviors to apply immediately

- Immutable original plan and additive amendments remain the source of truth.
- Compile every requirement into PlanAnchor → AC → evidence → execution task.
- Replace phase barriers with a cross-stage conflict-free READY queue.
- Workers use isolated branches/worktrees and explicit path/semantic ownership.
- Worker receipts are candidates, never final verdicts.
- Verification and review are independent and candidate-epoch bound.
- T2/T3 business, finance, auth, concurrency, migration, security and release
  claims receive adversarial specialist review.
- Browser/parity review binds exact REF/TGT states; no-vision and Playwright-only
  capabilities cannot overclaim visual or raw-CDP proof.
- Findings are consolidated by root cause into repair packs, then reverified.
- The main consumes bounded receipts and exact drill-down rather than raw child
  JSONL/full logs.
- Final reports are compiled from project ledger/evidence and cannot hide
  PARTIAL, stale, skipped, contradicted or capability-missing claims.

## 4. Adaptive shared-machine swarm

Inspect active agent/browser/test processes before dispatch. Until the global
broker is installed, the shared machine target is 8–10 active native children
across all three projects combined. Fill only remaining safe capacity.

The shared pool uses this composition:

| Active slots | Writers | Verifiers/test shards | Reviewers/auditors | Integration owner |
|---:|---:|---:|---:|---:|
| 8 normal | 4 | 2 | 1 | 1 |
| 10 burst | 5 | 2 | 2 | 1 |

When other projects are idle and resources are safe, this project may consume
more of the shared 8–10 slots. Do not start duplicate full builds, browsers,
CDP/MCP stacks or Compose topologies merely to reach a child count.

Shared heavy ceilings:

```text
full build/test: 2
browser-heavy: 2
full Compose: 1
```

Run independent implementation, verification and review concurrently across
stages. Do not make the main wait for one logical batch when unrelated READY
work exists.

## 5. Main-context economy before ToolOutputBroker is installed

- Raw test/native-child output stays with the child or in project evidence.
- Child receipts return artifact path/hash, command, exit code, counts,
  limitations, findings and next action.
- Main reads raw ranges only for semantic conflict, fidelity failure or final
  reconciliation.
- Do not invoke unbounded `opencode run --format json` into the main context.
- Token economy is advisory; never cut quality or stop work to satisfy it.

## 6. Continue current project to truthful completion

Revalidate current self-claims against exact source, raw evidence and immutable
candidate epoch. Treat existing DeepSeek/worker reports as candidate evidence,
not ground truth. Reopen every contradicted or capability-invalid acceptance
criterion and dispatch repair work while independent tasks continue.

Do not stop to ask whether to continue after repairable failures or review
rejection. Do not push/deploy/delete branches unless this project's existing
owner authority explicitly permits it.

Completion requires the current project's full effective plan to reconcile,
zero open blocking findings, fresh independent evidence, full-system topology
where the release claim requires it, and a truthful machine-derived final
report. Report separately:

```text
project completion state
behavioral dogfood status
full harness installation/enforcement status
remaining external authority or capability
```

Do not confuse successful behavioral dogfood with the latest Harness being
fully installed or certified.
