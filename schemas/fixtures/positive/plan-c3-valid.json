{
  "schema": "artifact/plan",
  "version": 1,
  "schema_version": "1.0.0",
  "repository_baseline": "main@a1b2c3d4e5f6",
  "intent_reference": "intent-20260726-001",
  "decisions": [
    {
      "id": "DEC-001",
      "statement": "Use RS256 for JWT signing",
      "rationale": "Industry standard for OAuth2, supported by all major libraries",
      "decided_by": "architect",
      "decided_at": "2026-07-26T09:00:00Z"
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "objective": "Implement Google OAuth2 callback handler",
      "requirements": ["REQ-001"],
      "file_ownership": ["src/auth/oauth2.ts", "src/auth/callback.ts"],
      "forbidden_files": ["src/legacy/auth.ts"],
      "acceptance_criteria": [
        "Google redirect produces valid JWT",
        "Invalid tokens are rejected with 401"
      ],
      "verification": {
        "proof_kinds": ["unit", "integration", "e2e"],
        "dimensions": ["correctness", "security"]
      },
      "evidence_requirements": ["jwt_validation_test_output", "e2e_success_screenshot"],
      "rollback": "Revert commit and rotate JWT signing key",
      "portability": {
        "platforms": ["linux", "darwin"],
        "constraints": ["node >= 18"]
      }
    },
    {
      "id": "T2",
      "objective": "Add JWT middleware for protected routes",
      "requirements": ["REQ-002"],
      "file_ownership": ["src/middleware/jwt.ts"],
      "acceptance_criteria": [
        "Protected routes return 401 without valid token",
        "Valid token passes through middleware"
      ],
      "verification": {
        "proof_kinds": ["unit", "integration"],
        "dimensions": ["correctness"]
      },
      "evidence_requirements": ["middleware_test_output"],
      "rollback": "Revert commit",
      "portability": {}
    }
  ],
  "completion_policy": "all_tasks"
}
